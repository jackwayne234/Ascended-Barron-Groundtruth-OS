# Ascended Barron: GroundTruth OS — X session entry point.
# Hand the session to the dashboard launcher (sets up env, WM, audio, then
# execs the dashboard). Runs as the normal user; no root assumptions.
exec /usr/local/bin/start-ai-os-gui-dashboard
